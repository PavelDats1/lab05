image:
  - Visual Studio 2022

configuration:
  - Release

before_build:
  - mkdir build
  - cd build

build_script:
  - cmake .. -G "Visual Studio 17 2022" -A x64
  - cmake --build . --config Release

test: off
